import styled from 'styled-components';
import { Container } from 'react-bootstrap';

export const AppContainer = styled.div``;

export const SingUpWrapper = styled(Container)`
  min-height: 100vh;
`;
export const SingUp = styled.div`
  width: 100%;
  max-width: 400px;
`;
